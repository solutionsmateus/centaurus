const elements = FileList(path)

const path = "centaurus/models"

function read_elements_in_html {
    document.getElementById("clique").addEventListener("click") {
        this.click.addEventListener("assai.py")
    }
}

function event_script {
    script = [0]
    for i in path:
        document.addEventListener("output.html")
        if element not exists{
            console.log("Not exists")
        }
    }


function 